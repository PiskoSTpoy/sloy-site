# Локальные служебные файлы — не для репозитория.
# _tools/.env хранит рабочий ключ Kling AI API — ни в коем случае не коммитить.
_tools/

# ОС-мусор
.DS_Store
Thumbs.db

# CI-артефакт GitLab Pages — генерируется в конвейере, не хранится в репо
/public/
